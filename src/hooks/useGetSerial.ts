import { useEffect, useState } from "react";
import { useSelector } from "react-redux";
import tvmazeService, { Series } from "../services/tvmazeService";
import { State } from "../store";

export function useGetSerial() {
 
}